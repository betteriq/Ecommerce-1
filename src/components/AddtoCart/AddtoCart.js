export const AddtoCart = (added) => {
  return {
    type: "Add",
    Added: added
  };
};
export const Exportet = (added) => {
  return {
    type: "Exportet",
    Added: added
  };
};
// export const Fade = (added) => {
//   return {
//     type: "Fade",
//     Added: added
//   };
// };